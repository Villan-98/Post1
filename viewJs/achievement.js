$(function(){/*
    let hv_div=$('#HV')
    refresh_div=function(data){
        console.log("data is "+data)
        $(hv_div).empty()
        $(hv_div).append(
            $(`<div id="hv_post">
                   Maximum votes on any Post are ${data}
                </div>
                
               `)
        )
    }
    window.abv=function() {       //val-but is 0 if it vome from dark thumb and vice versa
        console.log("like attach")
        $.get('/leader',((data)=>{
            console.log(data)
            refresh_div(data)
        }))
    }
    var myclose = false;*/

})